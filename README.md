# Alpha Guard (Vue js Project)

## Project setup

### Client
```
yarn install && yarn serve
```


### Server
to run the server and cd into the  `server` folder and run and install the dependencies by using

```
yarn install && yarn serve
```




